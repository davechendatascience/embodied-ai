# 凍結 VLA 的跨夾爪遷移

**進度報告。寫給沒有背景的外部讀者；所有數字皆為實測，並刻意保留失敗路徑，因為它們限縮了解空間。**

---

## 1. 問題

我們有一個凍結的視覺-語言-動作策略（VLA），只在 Franka Panda 手臂搭配 PandaGripper 上訓練過。
我們希望它能驅動**不同的夾爪**，不重新訓練、不微調、也不在目標本體上收任何示範。

**換手臂已經可行。** Panda → UR5e，保留 PandaGripper，在四個 LIBERO 套件上遷移成功率 11/12。
**換夾爪則不行。** UR5e + Robotiq85Gripper 與 UR5e + RethinkGripper 在本研究開始時，所有任務皆為 0/3。

問題在於：為什麼手臂是「免費」的，而夾爪不是；以及要救回夾爪，最小的介入是什麼。

### 設定

| | |
|---|---|
| 策略 | VLA-JEPA：Qwen3-VL-2B + V-JEPA2 編碼器 + DiT flow-matching 動作頭，凍結 |
| 輸入 | agentview RGB、腕部（eye-in-hand）RGB、語言、8 維狀態（末端位置 3、軸角 3、夾爪 qpos 2） |
| 輸出 | 7 維 `[world_vector(3), rotation_delta(3), open_gripper(1)]`，笛卡爾增量送入 OSC_POSE 控制器 |
| 模擬 | robosuite 1.4.1 / MuJoCo 3.1.6，LIBERO benchmark |
| 手臂 | Franka Panda（7 自由度）、Universal Robots UR5e（6 自由度） |
| 夾爪 | PandaGripper（來源）、Robotiq85Gripper、RethinkGripper（目標） |

---

## 2. 為什麼手臂能遷移、夾爪不能

策略對自身本體的唯一視野是腕部相機，而該相機安裝在**手臂的腕部連桿**上，不在夾爪上。因此：

```
camera → TCP，Panda + PandaGripper     [0, -50,  -97] mm
camera → TCP，UR5e  + PandaGripper     [0, -50,  -97] mm   ← 逐位元相同
camera → TCP，UR5e  + Robotiq85        [0, -50, -145] mm   ← 已破壞
```

策略學到的是驅動**相機**；TCP 之所以跟著到位，是因為訓練時 camera→TCP 是剛性常數。
換手臂保留了這個常數——手臂位於此不變量的**上游**。換夾爪則摧毀它。

自由度數目從未進入問題，因為策略下的是笛卡爾增量，由控制器去解 6 或 7 個關節。
Panda 的第 7 軸只提供零空間冗餘，而策略既不控制也不觀測它。

---

## 3. 限縮任何解法的實測結果

### 3.1 進入策略的所有非視覺通道都是關閉的

| 通道 | 量測 | 判定 |
|---|---|---|
| 語言 | 刻意給**錯誤**指令，動作只改變 cos 0.80 | 太弱，無法操控 |
| 本體感覺 | 兩手臂間整個狀態向量對調，輸出改變 **cos 1.000** | 完全被忽略 |
| 時序脈絡 | `horizon=0`；影像歷史 deque 是空的 | 回合內無法適應 |
| 動作分布 | `do_sample=True`，24 次取樣：散布僅 **±5 mm**，0/24 達到所需偏移 | 窄了 3–10 倍 |

動作頭是一個 diffusion sampler，但**預設關閉取樣**；開啟後散布幾乎沒有超過推論雜訊。
策略也是**每 7 步動作區塊查詢一次**，之後開迴路重播該區塊，因此它並不是緊密的視覺伺服。

**結論：視覺是唯一有頻寬的通道，而且無法「告訴」策略任何事。**

### 3.2 夾爪主宰了腕部影像

腕部影像中近黑像素比例，由上而下切成 8 個橫帶：

```
Panda      平均  3.4%   [1.7, 2.7, 3.4, 1.8, 3.6, 2.2, 12.0,  0.0]
Robotiq85  平均 25.8%   [0.5, 0.4, 0.5, 0.3, 2.3, 15.2, 89.2, 97.6]
```

Robotiq 把**畫面下方四分之一**完全遮黑——那正是策略決定閉合時，手指與「手指之間的物體」出現的位置。
它是在盲目狀態下做出閉合決策。這是那 48 mm 造成的投影效應：手更靠近鏡頭。

### 3.3 夾爪幾何（指墊面之間的自由間隙，不是質心距離）

```
                  閉合 → 張開          夾取點相對 TCP 偏移      閉合軸（末端座標系）
PandaGripper       0.00 → 79.00 mm        [0, 0, -3.6] mm         [1, 0, 0]
Robotiq85         11.65 → 99.39 mm        [0, 0, -9.2] mm         [1, 0, 0]
RethinkGripper    13.43 → 60.10 mm        [0, 0, -6.6] mm         [1, 0, 0]
```

三者的閉合軸在末端座標系中都是 `[1,0,0]`，且**跨不同手臂姿態的變異為零**——是真正與姿態無關的常數。
夾取點偏移的側向分量對三者都**恰為零**：任何對「包含工具軸之平面」鏡像對稱的雙指手，
其夾取中點在任何開合程度下都落在該軸上。這是結構性的，不是巧合。

---

## 4. 存活下來的規則：開口括夾（aperture bracketing）

對局部寬度為 `W` 的特徵做雙側抓取，需要

```
w_closed  ≤  W  ≤  w_open
```

沒有任何擬合參數。它預測了我們量到的每一個腳本化結果：

| 手 | 特徵 | 檢驗 | 預測 | 實測 |
|---|---|---|---|---|
| Panda | 碗緣 2.56 mm | 0 ≤ 2.56 ≤ 79 | 可抓 | 可抓（基線） |
| Robotiq85 | 碗緣 2.56 mm | 11.65 > 2.56 ✗ | **零接觸** | 三個初始狀態皆 0/0/0 接觸 |
| Robotiq85 | 碗身 90.9 mm | 11.65 ≤ 90.9 ≤ 99.39 | 可包覆，單側僅 4.2 mm 間隙 | 抓得住，但如履薄冰 |
| Robotiq85 | 湯罐 ~62 mm | 在括夾範圍內 | **不需偏移** | δ=0 時 27/30 格 |
| Rethink | 碗緣 2.56 mm | 13.43 > 2.56 ✗ | 不可行 | 0/37 |
| Rethink | 碗身 90.9 mm | 90.9 > 60.10 ✗ | 不可行 | 0/37 |

所以夾爪提供的是**門檻**；物體決定門檻是否被滿足。當來源與目標括住**同一個**特徵時，修正量為零；
當它們必須括住**不同**特徵時，側向修正量就是兩特徵之間的徑向差距。

**這是我們最想放到真實機器人前面的產出**：只要從夾爪 CAD 取兩個數字，加上對目標物的一次量測，
就能在**執行任何動作之前**判斷某隻手能不能接手某段示範，以及不能的話是為什麼。

---

## 5. 九種失敗的做法，以及失敗的原因

這些很重要，因為它們排除了整個家族的解法。

1. **固定的加法式動作偏置** —— 在**所有**尺度下都是 0/3。策略是回饋控制器；
   加法偏置只是移動閉迴路不動點，最後穩定在 `policy(image) = −bias` 之處。
2. **學習式修正器**（5k 參數 MLP，同姿態動作配對） —— 離線動作一致率 44.4% → 97.7%，
   閉迴路 **0/3**。在本專案中，離線一致率四度未能預測閉迴路成功。
3. **指尖摩擦係數對齊** —— Panda 有獨特的高摩擦 2.0 指墊；對齊後變成 0/3，**更差**。
4. **夾爪通道直通／正負號修正** —— 無效；所有夾爪都在 +1 時閉合。
5. **以公制開口下指令** —— 在此模擬器中是**無效操作**：robosuite 的 `format_action` 為
   `current + speed*np.sign(action)`，指令大小完全被丟棄。
6. **在模型中移動腕部相機**以還原 camera→TCP —— 得到 1/3，但這是沒有硬體對應的場景編輯，
   已作為解法被排除。
7. **雙向 shim**（本體感覺重標定 + 開口換算 + 相機↔手指混合） —— 各模式皆 0/3，
   不過混合確實讓物體最大抬升高度翻倍。
8. **交叉繪製（cross-painting）** —— 把真實夾爪從腕部畫面移除，合成一個渲染的 PandaGripper。
   達到 **1/3**，含一次真正的任務完成。失敗原因是：把代理手釘在真實夾取點，會使它比自然位置
   遠離鏡頭 44 mm，畫面佔比從真實的 19.0% 膨脹到 29.7%，導致三個初始狀態中有兩個導航失敗。
   放在自然深度則尺寸正確但失去修正。**影像與手指的衝突無法消除，只能搬家。**
9. **以夾取點對齊作為規則** —— 預測需 1.2 mm，實際需要 35 mm。直接被推翻。

---

## 6. 可行的架構

把問題反過來。與其欺騙策略去驅動目標，不如讓它驅動它認識的夾爪，再讓目標**跟隨**。

```
   VLA ──▶ 領導端 LEADER （來源夾爪、真實觀測、完全在分布內）
                │  TCP + 旋轉 + 夾爪指令
                ▼
          修正層  ◀── 兩隻手的即時指墊幾何
                │
                ▼
           跟隨端 FOLLOWER （真實目標：無相機、無策略，只收笛卡爾目標）
```

目標端的腕部相機**完全不被使用**，因此遮擋、48 mm 過衝、代理手尺寸衝突全部在設計上被排除。

### 修正量

```
goal_follower(t) = tcp_leader(t) + R_ee(t) · offset_ee
```

以**末端執行器座標系**表示，因此會隨手腕旋轉。若以世界座標表示，只在當初量測的那一個接近角度下有效，
換一個任務就會無聲地失效。

| 項 | 來源 | 此處數值 |
|---|---|---|
| 軸向 | `pinch_ee(來源) − pinch_ee(目標)`，每步由即時指墊幾何重新計算 | 約 6 mm |
| 側向 | 來源所括特徵與目標所能括特徵之間的徑向差距，沿目標自身的閉合軸 | 45 mm（碗）、0（罐） |
| 正負號 | **無法推導** | 一個位元 |

所有夾爪端的量都在執行期從實際安裝的硬體讀出；換一隻手，數字自己就會變。
唯一與物體相關的輸入是特徵寬度。

### 硬性實作限制

**每個行程只能有一個 MuJoCo 環境。** 建立第二個 EGL context 會讓第一個的渲染變成未初始化記憶體——
在完全相同的姿態下，腕部影像近黑比例從 27.6% 變成 95.0%。
`make_current()` 修不好、robosuite 的觀測路徑繞不過、顛倒建立順序沒用、關閉第二個環境的相機也沒用：
**損害發生在建構當下**。在正視此點之前，我們建了三套測試框架、丟棄了三組結果。
現在跟隨端跑在自己的行程裡，且從不渲染。

---

## 7. 結果

凍結的 Panda-only VLA、UR5e 手臂、目標夾爪跟隨：

| | 湯罐 | 雙物件（長時程） | 碗 | 抽屜 |
|---|---|---|---|---|
| **Robotiq85Gripper** | **3/3** | **3/3** | 0/3 | 0/3 |
| **RethinkGripper** | **2/3** | 1/3 | 0/37（腳本化） | — |

- `libero_object/0` —— 「拿起字母湯罐並放進籃子」，抬升 179–215 mm
- `libero_10/0` —— 「把字母湯罐**和**番茄醬**都**放進籃子」，兩次連續抓取，230–250 步，
  修正量在整個序列中維持有效
- 追蹤誤差 2.8–3.5 mm；同一性對照組（Panda 跟隨 Panda、零偏移）為 3/3

**這個分界是先預測、後量測的**，來自開口括夾規則。會成功的兩個任務，正是目標手真正有餘裕的兩個。

---

## 8. 未解問題

1. **碗（0/3）。** 碗緣**低於** Robotiq 的閉合間隙，因此根本無法夾。碗身**可以**包覆，
   但單側只有 **4.2 mm 間隙**（99.4 mm 開口 vs 90.9 mm 碗身）——不論控制多努力，手都會在下降時刮到它。
   增加子步數反而讓追蹤誤差**更不穩定**，因為那些尖峰是碰撞而非延遲。
   這可能是一個處於可行性邊緣的物體，而非方法失效，但我們尚未證明這點。

2. **抽屜（0/3），而且是不同的體制。** 物體路徑直線度清楚地把它們分開：
   抽屜的領導端路徑直線度為 **1.00**（滑動關節），罐子則是 0.73（自由弧線）。
   一旦手接合抽屜，它就與一個 1 自由度關節產生運動學耦合，沒有任何餘裕去吸收偏移——
   誤差變成力的對抗，跟隨端因此卡死（追蹤中位數 137 mm、峰值 288 mm）。
   **受約束接觸任務需要沿約束軸的順應性，而不是位置偏移。**
   開口判準對**鉤取（hooking）**也完全沉默，而欠致動手能鉤、平行夾爪不能。
   該任務的領導端基線本身只有 1/3，必須先修好才能談遷移。

3. **側向修正的正負號無法推導。** 大小與軸向可以，正負號是一個位元。
   「試了再驗證」在成本上可行——失敗的嘗試會使物體位移約 12 mm（碗上最大 17.7 mm，0/18 超過 20 mm）——
   但這些嘗試**並不獨立**，因為 12 mm 大約就是抓取穩定區的寬度本身。

4. **開迴路。** 目標端沒有任何回饋，因此掉落的物體永遠不會被重試。
   我們認為解法是把跟隨端的**物體狀態**餵回領導端的場景（而非策略），
   讓策略看到物體還在原處而自然重試，完全不需要知道 Robotiq 是什麼。
   （**更新：此項已實作。** 僅在抓取前鏡像；全程鏡像會讓罐子任務從 3/3 掉到 1/3，
   因為兩手都持有物體時，鏡像會與領導端的接觸求解器對抗。
   限制在抓取前之後，閉迴路是免費的，並讓碗的抬升從 0.0 mm 變成 85.1 mm。）

5. **範圍界定。** 策略是在**模擬的來源機器人**上執行任務，由目標端實際動作。
   這是「帶本體修正的軌跡遷移」，不是凍結策略直接驅動目標。
   在真實硬體上，這需要一個由感知餵入場景狀態的來源本體孿生體。

6. **附帶碰撞。** 即使在成功的回合中，目標端仍會碰倒來源端可以避開的鄰近物體——
   它的手指佔據更多空間，而接近路徑並未考慮鄰居。

---

## 9. 方法論筆記

記錄下來，因為它們花掉了實際時間，也會讓讀者付出同樣代價。

- **離線一致率無法預測閉迴路成功。** 四次發生。某修正器達到 97.7% 動作一致率，閉迴路 0/3。
- **在解讀分數之前，先診斷軌跡。** 某個 0/3 被當成推翻先前結果的證據，
  但當時手臂根本沒靠近物體 57 mm 以內——那是控制迴路壞了，不是發現。
  另一個 0/3 則是迴圈提前中斷，掩蓋了兩次成功。
- **要看影片。** 本專案最大的一次修復——跟隨端的場景從未與領導端對齊，
  導致兩隻機器人**在不同的場景佈局中**完美地互相追蹤——
  在所有 log 欄位中都看不出來（追蹤誤差顯示 3.6 mm），但在一張畫面裡一目了然。
- **確認指標到底在量什麼。** 碗任務的接觸數與抬升高度，實際上是對著
  `flat_stove_1_burner_plate` 計算的，因為指令中「plate」出現兩次。
  只因為一個**成功**的領導端顯示物體位移 0.0 mm 才被抓到。
- **量幾何，不要量參數化。** 一個 35.3 mm 的寬度常數，後來發現是兩顎**質心**距離，
  而非指墊面之間的自由間隙。它連自己的對照都通不過——
  它要求 Panda 對一個它在零偏移下就能抓的碗緣做 16.5 mm 偏移——
  之所以「有效」，只是因為它剛好落在一個有效區間內。

---

## 10. 我們最希望得到建議的方向

1. **受約束接觸體制。** 有沒有原則性的方法，能在夾爪之間遷移一段穿過運動學約束
   （抽屜、門、把手）的示範，而不必在目標端上完整的力控？
2. **在不依賴感知的情況下決定正負號**，或找到比「試了再驗證」更好的搜尋方式——
   考慮到每次嘗試對場景的擾動量，大約就等於容差本身的寬度。
3. **在不破壞領導端穩定性的前提下閉合迴路**——如何讓來源本體孿生體安全地觀測目標端的世界。
4. **開口判準能否推廣到雙顎手之外**：它是為鏡像對稱的雙指夾爪推導的，
   對鉤取、吸盤、多指手完全沉默。
5. **那個 4.2 mm 間隙的案例。** 如履薄冰的包覆式抓取值不值得追？
   還是正確答案是宣告它不可行，然後換一個抓取方式？
