"""The simulator side: opening a LIBERO task, executing an action (servo, gripper servo),
resetting scenes, the scene model, and every contact query. Both stacks execute through
sim_arm.SimArm, so teacher and student act through one path.
"""
