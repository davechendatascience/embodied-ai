"""Pure geometry: rotations and poses (frames: NumPy; se3: torch), product-of-exponentials
kinematics (poe, kinematics, kin_np, ik), the action interface and the student's tool state.
kin_np and frames are held to their torch references by tests/.
"""
