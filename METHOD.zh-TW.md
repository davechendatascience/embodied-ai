# 跨形體遷移方法：以幾何對齊取代重新訓練

本文把專案目前**已被量測驗證**的方法整理成可實作的流程，並提出下一階段
「避障」的設計——特別是機器人**辨識自身構型**（self-configuration
recognition）這一塊，因為它是本方法從「換手臂」走向「換手臂＋換場景」的關鍵缺口。

閱讀順序建議：`README.zh-TW.md` 記錄的是**實驗過程與被推翻的假設**；本文只留下
**站得住腳的結論與作法**。

---

## 1. 核心命題

一個在 Franka Panda 上訓練的 VLA（此處為 VLA-JEPA）要在 UR5e 上執行同一個任務，
直覺上需要處理「形體差異」。但量測顯示，真正擋住遷移的**不是外觀差異，而是構型
差異（configuration OOD, not appearance OOD）**：

- LIBERO 的每一條示範都從**同一個 home pose** 出發。
- 因此策略對「其他起始構型」**沒有任何訓練訊號**。
- UR5e 預設起始位置離 Panda 的起始 TCP 有 **178 mm**，從第一格畫面就在分佈外。

把 UR5e 的起始 TCP 用 IK 對齊到 Panda 的起始 TCP 之後：

| suite | arena | Panda | UR5e（已對齊） |
|---|---|---|---|
| `libero_object/0` | floor | 3/3 | 3/3 |
| `libero_spatial/0` | table | 3/3 | 3/3 |
| `libero_10/0` | living room | 3/3 | 3/3 |
| `libero_goal/0` | table | 3/3 | 2/3 |

**11/12。零參數、零訓練、零規劃器。**

這個結果推翻了本專案原本的論點（需要 keypose 抽象 + cuRobo 規劃 + 學習式
adapter）。誠實地說：那些元件在這個基準上**不是必要的**。

### 1.1 為什麼「對齊起始位姿」就夠了

因為這個策略基本上是在做**視覺伺服（visual servoing）**：

- 交叉輸入實驗：把兩支手臂的本體感覺（proprioception）互換，輸出的
  cosine 相似度是 **1.000**——策略**完全忽略本體感覺**。
- 也就是說，策略只看影像。只要影像在分佈內，它就會把手臂開回正確位置。
- 每一格都重新觀測，所以**小誤差會被自然吸收**，不需要精準的軌跡追蹤。

這也解釋了為什麼 450 筆配對資料訓練出的修正器（corrector）**是有害的**：
approach 階段兩臂的動作分歧是 **0.0%**，去修正一個本來就正確的訊號只會加入雜訊。

---

## 2. 方法：三個對齊，而非一個模型

方法可以總結成一句話：**讓「策略看到的幾何」回到訓練時的樣子，其餘不要動。**

### 2.1 對齊之一：起始構型（已驗證）

用位置控制把目標手臂的 TCP 開到來源手臂的起始 TCP：

```python
site = m.site_name2id(env.env.robots[0].controller.eef_name)
for _ in range(250):
    dp = START - d.site_xpos[site]
    if np.linalg.norm(dp) < 0.004:
        break
    env.step(np.concatenate([np.clip(dp / 0.05, -1, 1), np.zeros(3), [-1.]]))
```

`START` 取自來源手臂成功 rollout 的第一格 TCP。收斂門檻 4 mm，實測誤差 2.5 mm。

### 2.2 對齊之二：狀態向量寬度（實作必需）

策略的狀態輸入是**定寬**的。Panda hand 有 2 個手指關節，Robotiq85 有 6 個，
狀態向量會從 8 維變成 12 維，伺服器直接拒絕。

因為已經量測到**策略忽略本體感覺**，用夾爪開度合成一個 Panda 形狀的 2 維向量是
忠實的替代，而不是造假：

```python
def grip2(qpos):
    q = np.asarray(qpos, float).ravel()
    if q.size == 2:
        return q
    w = float(np.abs(q).max())
    return np.array([w / 2.0, -w / 2.0])
```

> 這一步的合法性**完全建立在那個 cos 1.000 的量測上**。若換成一個真的會讀取本體
> 感覺的策略，這個捷徑就不成立，必須改為重新訓練狀態編碼器。

### 2.3 對齊之三：腕部相機外參（部分有效，見 §3）

腕部相機掛在**手腕連桿**上（`pos="0.05 0 0"`），Panda 與 UR5e **完全相同**。
所以換夾爪並不會移動相機，而是把 TCP 從相機旁邊推遠：

| 夾爪 | flange→TCP | 腕相機→TCP |
|---|---|---|
| PandaGripper | 0.0970 m | 0.1091 m |
| Robotiq85Gripper | 0.1450 m | **0.1534 m** |

對一個靠視覺伺服的策略，相機到抓取點的距離**就是它的標尺**。距離變長 44 mm，
物體在畫面中看起來就更遠，策略會持續前進，把 TCP 開過頭。

修正方式是把相機沿著視線滑回去，讓「相機→TCP」回到訓練時的 0.1091 m
（`xembody` 之外，實作於 `examples/libero_ur5e.py: align_wrist_camera`）。
在真實機器人上，相機外參本來就是自由的設計選擇，所以這是**還原訓練幾何**，
不是事後補償。

---

## 3. 目前的邊界：夾爪換掉之後

把 UR5e 的夾爪從 PandaGripper 換成它原生的 Robotiq85，在同樣的起始對齊之下：

| 設定 | 成功 | TCP→碗最近距離 | 碗被抬起 |
|---|---|---|---|
| PandaGripper | ✅ 66 步 | 40.1 mm | 77.2 mm |
| Robotiq85 | ❌ 300 步 | 26.8 mm | 5.6 mm |
| Robotiq85 ＋ 相機對齊 | ❌ 300 步 | 21.1 mm | 31.9 mm |

讀法：

- **「戳太深」是真的**。Robotiq85 把 TCP 開到比 Panda 的抓取點深 13–19 mm。
- **相機對齊有效但不足**。抬起高度從 5.6 mm 改善到 31.9 mm（約 5.7 倍），
  但仍然抓不起來。
- 因此**殘餘誤差不是感知問題**，而是夾爪本身的機構差異：Robotiq85 的手指是
  **弧線閉合**而非平行夾持，可抓取的姿態集合與 Panda hand 不同。

這是目前這套「零參數幾何對齊」方法**明確的失效邊界**，也是唯一需要引入學習成分
的地方。對應的資料集正在收集：配對資料中加入**夾爪幾何欄位**
`geom = [flange→TCP, 腕相機→TCP]`，以差值形式進入特徵，讓同一個修正器能服務
多種夾爪——若這一欄是常數，模型只會把某一支夾爪的偏移學成偏置，然後錯誤地套用到
所有其他夾爪上。

---

## 4. 避障：下一階段設計

目前所有已驗證的結果都是**無避障**的：策略直接輸出末端增量，沒有任何碰撞檢查。
在 LIBERO 的桌面任務上這可行，因為示範本身就是無碰撞的。一旦要換場景、換基座
高度、或加入非示範中的障礙物，就必須把可行性（feasibility）這一層補回來。

設計原則沿用本專案的分工：

- **Intent（VLA）**：提供語意上「要去哪裡」的 SE(3) 目標。
- **Feasibility（cuRobo）**：決定「怎麼去」——運動學、碰撞、關節極限。

### 4.1 環境障礙：從深度而非從模擬器幾何

不使用特權資訊（privileged information）。障礙世界必須由深度影像重建：

```
depth + K + T_world_cam  →  backproject()  →  點雲
                          →  drop_spheres()  →  移除機器人自身的點
                          →  voxelise()      →  cuRobo cuboid 障礙
```

已實作於 `xembody/world.py`。關鍵參數：`VOXEL_M = 0.03`、`DIMS_SCALE = 0.7`、
`MAX_OBSTACLES = 1536`。`DIMS_SCALE` 讓 cuboid 略小於格距，否則相鄰體素會相互
重疊、把可行的窄縫封死——這是實測中「為什麼永遠 infeasible」的主因之一。

抓取路點需要**豁免目標物體**的碰撞，否則規劃器不可能把夾爪開到物體上。

### 4.2 自身構型辨識（self-configuration recognition）

這是本節的重點，也是目前最大的缺口。

**問題**：從腕部相機與外部相機重建出來的點雲裡，**有相當比例是機器人自己**——
手臂連桿、夾爪手指、甚至基座。如果直接把這些點當成障礙送進 cuRobo：

1. 機器人會把**自己**視為障礙，於是任何動作都碰撞，規劃永遠 infeasible。
2. 換一支手臂或換一個夾爪，被誤判為障礙的形狀就完全不同，
   **避障行為會隨形體而變**——這恰好摧毀了本方法的跨形體性質。

目前 `drop_spheres()` 的作法是用一組球體把機器人自身的點移除。這在已知構型下可行，
但它**依賴外部提供正確的關節角與正確的球體模型**。要讓這件事在跨形體下成立，
需要三個層次：

**(a) 幾何層：由 URDF 正向運動學生成自身遮罩**

- 從 URDF ＋ 當前關節角做 FK，得到每個連桿的位姿。
- 用內接球（`examples/make_robot_cfg.py`，`RADIUS_PCT = 65`）近似連桿體積。
- 對點雲做球體剔除，並**外擴一個 margin** 吸收 URDF 與實際幾何的誤差。
- 這一層必須包含**夾爪**：Robotiq85 的手指比 Panda hand 長 48 mm，
  漏掉手指會在抓取瞬間把夾爪自己判成障礙——也就是**最需要精度的那一刻失效**。

**(b) 一致性層：自身遮罩必須與 TCP 定義同源**

自身模型、TCP 定義、相機外參三者必須來自**同一份幾何來源**。本專案已經有
`xembody/contract.py` 在做這件事（`Frame`、`require_compatible()`），它曾在
0.4 秒內抓出一個 912 mm 的基座錯誤。避障要沿用同一套契約：
**任何自身遮罩都必須宣告它是用哪一份 URDF、哪一組關節角、哪一個 TCP 產生的**，
不一致就直接拋錯，而不是安靜地產生一個錯誤的障礙世界。

**(c) 驗證層：自身遮罩的正確性要能被量測**

建議的量測（尚未實作）：

- **殘餘自身點比例**：在**已知無障礙**的場景中重建點雲，剔除自身後剩餘點數應趨近
  於零。這是一個不需要標註的自我檢查。
- **過度剔除檢查**：放一個已知位置、已知尺寸的物體在手臂附近，
  確認它**沒有**被自身遮罩吃掉。過度剔除比不足剔除更危險，因為它讓真障礙隱形。
- **跨形體不變性**：同一個場景、同一組障礙，換成另一支手臂後，
  重建出的**環境**障礙集合應該幾乎相同。若差異顯著，代表自身遮罩把形體差異
  洩漏進了環境模型。

這第三點是本方法的核心檢驗：**環境模型必須與形體無關**。做到了，避障才是可遷移
的；做不到，就只是替每支手臂各自調參。

### 4.3 與現有結果的接合方式

避障**不應該**取代已驗證的視覺伺服路徑，而是作為一層**安全濾網**：

- 策略照常每格輸出末端增量。
- 在送進控制器之前，用 cuRobo 檢查該增量是否會造成碰撞（自身或環境）。
- 若安全，原樣通過——這樣在 LIBERO 桌面任務上會**完全還原目前 11/12 的結果**。
- 若不安全，才投影到最近的可行方向，或觸發一次局部重規劃。

這個順序很重要：目前的證據顯示規劃器**不是**成功的必要條件，所以它必須以
「預設不介入」的形式加入，並且要能量測介入率。若介入率在 LIBERO 上不接近 0，
代表避障層本身有問題，而不是策略有問題。

---

## 5. 方法的適用範圍與限制（誠實版）

**成立**：

- 換手臂（Panda → UR5e），同夾爪：11/12，零參數。
- 機制已被量測：策略忽略本體感覺、僅靠視覺伺服、每格重新觀測。

**目前不成立**：

- 換夾爪（PandaGripper → Robotiq85）：0/6。相機對齊改善但不足。
- `libero_goal` init7：12 次中唯一的失敗，原因尚未查明。

**已被推翻、不要再引用的說法**（詳見 `README.zh-TW.md`）：

- 2.8× 動作幅度膨脹（n=1 的假象）
- 自我遮擋機制
- 漂移累積模型
- 「UR5e 構型上搆不到」

**尚未驗證**：

- 完整 LIBERO 基準（目前僅每個 suite 的 task 0）
- 深度重建的避障（`xembody/world.py` 已實作，端到端未驗證）
- 自身構型辨識（§4.2，設計階段）
