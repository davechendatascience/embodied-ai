diff --git a/constraint_generation.py b/constraint_generation.py
index cbc5d5e..c4c5e77 100644
--- a/constraint_generation.py
+++ b/constraint_generation.py
@@ -1,5 +1,4 @@
 import base64
-from openai import OpenAI
 import os
 import cv2
 import json
@@ -8,6 +7,8 @@ import numpy as np
 import time
 from datetime import datetime
 
+from rekep_libero.vlm_backends import make_backend
+
 # Function to encode the image
 def encode_image(image_path):
     with open(image_path, "rb") as image_file:
@@ -16,7 +17,7 @@ def encode_image(image_path):
 class ConstraintGenerator:
     def __init__(self, config):
         self.config = config
-        self.client = OpenAI(api_key=os.environ['OPENAI_API_KEY'])
+        self.backend = make_backend(config)
         self.base_dir = os.path.join(os.path.dirname(os.path.abspath(__file__)), './vlm_query')
         with open(os.path.join(self.base_dir, 'prompt_template.txt'), 'r') as f:
             self.prompt_template = f.read()
@@ -134,19 +135,8 @@ class ConstraintGenerator:
         cv2.imwrite(image_path, img[..., ::-1])
         # build prompt
         messages = self._build_prompt(image_path, instruction)
-        # stream back the response
-        stream = self.client.chat.completions.create(model=self.config['model'],
-                                                        messages=messages,
-                                                        temperature=self.config['temperature'],
-                                                        max_tokens=self.config['max_tokens'],
-                                                        stream=True)
-        output = ""
-        start = time.time()
-        for chunk in stream:
-            print(f'[{time.time()-start:.2f}s] Querying OpenAI API...', end='\r')
-            if chunk.choices[0].delta.content is not None:
-                output += chunk.choices[0].delta.content
-        print(f'[{time.time()-start:.2f}s] Querying OpenAI API...Done')
+        # query whichever VLM backend is configured
+        output = self.backend.query(messages)
         # save raw output
         with open(os.path.join(self.task_dir, 'output_raw.txt'), 'w') as f:
             f.write(output)
diff --git a/main.py b/main.py
index f0c0de3..e697bd5 100644
--- a/main.py
+++ b/main.py
@@ -3,15 +3,24 @@ import numpy as np
 import json
 import os
 import argparse
-from environment import ReKepOGEnv
 from keypoint_proposal import KeypointProposer
 from constraint_generation import ConstraintGenerator
-from ik_solver import IKSolver
 from subgoal_solver import SubgoalSolver
 from path_solver import PathSolver
-from visualizer import Visualizer
 import transform_utils as T
-from omnigibson.robots.fetch import Fetch
+
+# OmniGibson-only imports. Made lazy so the loop logic below can be reused
+# against another simulator (see src/rekep_libero/) without Isaac Sim
+# installed; `visualizer` additionally needs open3d, which has no aarch64
+# wheel. Main.__init__ still requires them, so the original demo is unaffected.
+try:
+    from environment import ReKepOGEnv
+    from ik_solver import IKSolver
+    from visualizer import Visualizer
+    from omnigibson.robots.fetch import Fetch
+except ImportError as _og_import_error:  # pragma: no cover
+    ReKepOGEnv = IKSolver = Visualizer = Fetch = None
+    _OG_UNAVAILABLE = _og_import_error
 from utils import (
     bcolors,
     get_config,
diff --git a/utils.py b/utils.py
index 60672e6..ad670eb 100644
--- a/utils.py
+++ b/utils.py
@@ -1,7 +1,6 @@
 import os
 import numpy as np
 from numba import njit
-import open3d as o3d
 import datetime
 import scipy.interpolate as interpolate
 from scipy.spatial.transform import Slerp
@@ -423,10 +422,20 @@ def get_linear_interpolation_steps(start_pose, end_pose, pos_step_size, rot_step
 def farthest_point_sampling(pc, num_points):
     """
     Given a point cloud, sample num_points points that are the farthest apart.
-    Use o3d farthest point sampling.
+
+    Numpy reimplementation of o3d's `farthest_point_down_sample`, which is the
+    only thing this file used open3d for. open3d publishes no aarch64/py3.12
+    wheel, and dropping it removes the single hardest dependency in the repo.
+    Matches o3d's greedy algorithm, including seeding from index 0.
     """
     assert pc.ndim == 2 and pc.shape[1] == 3, "pc must be a (N, 3) numpy array"
-    pcd = o3d.geometry.PointCloud()
-    pcd.points = o3d.utility.Vector3dVector(pc)
-    downpcd_farthest = pcd.farthest_point_down_sample(num_points)
-    return np.asarray(downpcd_farthest.points)
\ No newline at end of file
+    if num_points >= pc.shape[0]:
+        return pc.copy()
+    selected = np.empty(num_points, dtype=np.int64)
+    selected[0] = 0
+    # running min distance from every point to the selected set
+    dist = np.linalg.norm(pc - pc[0], axis=1)
+    for i in range(1, num_points):
+        selected[i] = int(np.argmax(dist))
+        dist = np.minimum(dist, np.linalg.norm(pc - pc[selected[i]], axis=1))
+    return pc[selected]
\ No newline at end of file
